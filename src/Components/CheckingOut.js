import React from 'react';

class CheckingOut extends React.Component {
    constructor(props) {
        super(props);
        this.state = {

        };
    }
    render() {
        // var list = [];

        // for (var i = 0; i < this.props.items.length; i++) {
        //     list.push(
        //         <ChatItem key={i} onLeftSide={this.props.items[i].onLeftSide}
        //             chatItemText={this.props.items[i].chatItemText}></ChatItem>)
        // }

        return (
            <div>This is the Cashier.</div>
        );
    }
}


export default CheckingOut;
